# KickBazar ToC Web PRD — 文档洁癖审查报告（v1.2）

> 审查对象：飞书 PRD 终稿（用户粘贴版）  
> 对照基准：v1.1 / v1.1.1 / v1.1.2 / v1.1.3 / v1.2 / v1.2.1 / v1.2.2 已确认需求  
> 审查维度：自相矛盾、重复块、过期表述、结构错位、笔误、验收不可测  
> **状态（2026/09/21）：** 修订已落盘 → [`kickbazar-toc-web-prd-feishu-clean.md`](./kickbazar-toc-web-prd-feishu-clean.md)（飞书粘贴指南）+ [`kickbazar-toc-web-prd.md`](./kickbazar-toc-web-prd.md)（同步版）

---

## 一、审查结论（Executive Summary）

| 等级 | 数量 | 说明 |
|------|------|------|
| **P0 阻断** | 18 | 研发/测试会按错做；必须改 |
| **P1 脏数据** | 12 | 重复段落、飞书占位、下期内容混在本期 |
| **P2 笔误/格式** | 9 | 错别字、标点、表格列错位 |

**总体评价：** 功能点已基本齐，但 **v1.0 原文与 v1.1–v1.2 补丁叠加未清理**，同一规则在 3.4 / 4.1 / 4.6 / 4.9 多处互相打架。建议按下方「§A 必改清单」逐段替换，**不要只追加表格**。

---

## 二、P0 矛盾清单（必须改）

### P0-1｜§1.2 范围表重复且冲突

**现象：** 出现两个「分类」行——一个写「不含 L3 Filter」，一个写「Categories Filter」。  
**应以 v1.2 为准：** 分类页 **有** Categories Filter（L1 页=L2 跳转；L2 页=L3 筛选）。  
**另：** 第二个表「Categories Filte」缺字；行末多字符 `r`；「全局 Sticky」应合并进第一个表，勿留孤表。

---

### P0-2｜§1 文档基础信息缺版本史

**现象：** 仅有 v1.0 一行。  
**应补：** v1.1 / v1.1.1 / v1.1.2 / v1.1.3 / v1.2 变更记录（见 §C.1）。

---

### P0-3｜§2.4 第 5 点自相矛盾

**现象：**  
- 本期做：「分类页 Categories Filter」  
- 本期不做：「搜索结果页/**分类页**类目 Filter（L1–L3）」  

**修正：** 本期不做仅限 **搜索页** Filter；分类页 Filter **本期做**（L2 跳转 / L3 本页筛选）。

---

### P0-4｜§3.2 业务目标仍写「游客购物车合并」

**现象：** 「登录后游客购物车自动合并」与 §2.4-6、v1.1.3 **取消游客车** 冲突。  
**修正：** 改为「Web 须登录加购；购物车数据仅存账号，不做游客 localStorage / 合并」。

---

### P0-5｜§3.4 衔接规范大量过期 + 重复行

| 过期/错误行 | 问题 | 处理 |
|------------|------|------|
| 分类一级收敛入口（抽屉 L1 进 L1 页） | 与 v1.2 抽屉 L1 不跳转冲突 | **删除**，改用抽屉三行规则 |
| PDP 加购（游客 BR832） | 与 v1.1.3 须登录加购冲突 | **删除**，只保留「PDP Add to Cart」行 |
| 购物车「去结算」出现两次 | 重复 | **合并为一行** |
| 未登录访问结算/订单/地址 | 缺 `/cart` | 补上 `/cart`（BR852） |

---

### P0-6｜§4.0 BR 复用索引 — BR120 行错位

**现象：** 「适用范围」列写成了 BR847 验收文案。  
**修正：** `所有商品网格/列表（点击主体→PDP；topicTag→BR847）` 或恢复标准表述。

---

### P0-7｜§4.1 与抽屉/购物车规则过期

| 位置 | 问题 |
|------|------|
| 功能描述末段 | 「抽屉 L1 点击与次导航一致进 L1 页」— **错** |
| 登录态设计-2 | 「购物车按规则 9」— 规则 9 已删 |
| 业务规则-6 退出登录 | 写跳转 `loginUrl`，BR821 写跳转 `/` — **不一致，以 BR821 为准** |
| BR105 验收 | 仍含「L1 Click 进 L1 页」— **错** |
| BR106 | 未区分登录态；未登录不应加购更新角标 |

---

### P0-8｜§4.5 搜索排序本期/下期混乱

**现象：** 同章同时存在：  
- 「下期 Recommend 四档」  
- 「本期 From Z-A / A-Z / Newest / Oldest」  
- sort 枚举表仍是 recommend/price/sales  
- 「本期不做 A-Z」与「本期默认 A-Z」矛盾  

**修正原则（按你文档意图）：**  
- **本期与 App 一致：** Sort By = From Z-A（默认）、From A-Z、Newest first、Oldest first  
- **下期：** Recommend / New Arrival / Price / Sales 四档（整表移入 §6.2，本期 BR211 只验收 4 档 App 排序）  
- 删除重复的 business rule 第 5 点、合并 BR211 只保留一行

---

### P0-9｜§4.6 分类抽屉 + Filter + 路由约定冲突

| 位置 | 问题 |
|------|------|
| 路由约定 | 「抽屉 L1、L2 View All 均收敛 L1 页」— L1 不跳转、L2 项进 L2 页 |
| 业务规则 ②③ | 标【下期】但正文 L3 Filter 已写本期 |
| BR833 | 标【下期】与 BR836（本期 L3 筛选）重复冲突 |
| FL142c | 「L1 页 L2/L3 级联」— L1 页仅 L2 |

---

### P0-10｜§4.7 PDP 仍写「游客可加购」

**现象：** 4.7.2 页面模块、4.7.11 内嵌 BR 表（整段错位）、BR406 一行写「游客可用」。  
**处理：** 统一为 v1.1.3；**删除 4.7.11 中误贴的 BR401–BR421 交互表**（应只在 4.7.4）。

---

### P0-11｜§4.7 BR851「登录合并截断」

**现象：** v1.1.3 已取消游客车，BR851 失去前提。  
**处理：** **删除 BR851** 及购物车规则 16；SKU 上限仅针对已登录账号购物车。

---

### P0-12｜§4.9.1 购物车规则 16 + 飞书占位

**现象：** 规则 16「登录合并截断」；「暂时无法在飞书文档外展示此内容」占位未清。  
**处理：** 删规则 16；英文 Copy 表单独附录或链到 changes §12.1。

---

### P0-13｜§4.9.6 地址布局仍为「两列」

**现象：** 正文与 BR620b 仍为 ToB **两列**；v1.2 已定 **三行**（上姓名电话 / 中地址截断 / 下省市区）。  
**处理：** 全文统一三行；Change 列表卡片同布局。

---

### P0-14｜§4.9.6 Order Summary 过期

**现象：** 仍写 Coupon 行、Shipping Fee 固定 60；与「本期无 Coupon」「运费动态三档」冲突。  
**处理：** Order Summary 仅：Subtotal + Promotion + Shipping Fee（动态）+ COD Fee（划线 Free）。

---

### P0-15｜§4.9.6 优惠券大段

**现象：** 标注「本期不做」但保留完整交互规格（易误导开发）。  
**处理：** 正文只留一句「见 §6.1」；详细 BR625–626 移入 §6.2 P1。

---

### P0-16｜§4.9.7 shippingFee 字段说明粘贴错误

**现象：** `shippingFee` 的「字段说明」粘成了 Order Summary 公式段落。  
**修正：** `按 App 规则动态计算的运费，单位 BDT`。

---

### P0-17｜§7.1 验收自相矛盾

**现象：** 同节既有「分类页均不展示 Filter」又有「L1/L2 Filter」；仍有「游客购物车合并」。  
**处理：** 用 §C.4 替换整段验收清单。

---

### P0-18｜NFR007 与 v1.2 冲突

**现象：** 「分类页不含 Filter」。  
**修正：** 「搜索页不含 Filter/Featured；**分类页含 Categories Filter（L2 跳转/L3 筛选）**」。

---

## 三、P1 脏数据（建议清理）

1. **§1.2** 第二个孤立模块表 → 合并进主表后删除  
2. **§3.4** 重复「购物车去结算」→ 保留 1 行  
3. **§4.5** business rules 两个「5.」→ 重新编号  
4. **§4.7.11** 标题是「字段定义」内容是「交互说明」→ 拆回 4.7.4 / 4.7.12  
5. **§4.9.6** 「结果页」描述出现两次 → 合并  
6. **§4.9.8** BR620 与上文 BR620a–c 重复 → 保留 BR620a–c + BR853，删底部重复块  
7. **§4.1** 账户菜单「至少五项」与现 6 项不一致 → 改「至少包含上表菜单项」  
8. **§4.3** BR112 About Us 字段名 `aboutKickbazarUrl` vs 验收 `aboutUsUrl` → 统一  
9. **§4.9.4** BR618 仍标 `未登录●` → 删除（/cart 已登录门禁）  
10. **§4.9.0 BR641** 「未登录走 BR619」→ /cart 仅登录可进，改为「用户在 /cart 时已登录；401 走 BR804」  
11. **附录** FL139 标下一期但仍列 P0 范围 → 附录注明「下期」  
12. **§6.3 P2** 游客合并已标注关闭 → 可移入「已关闭决策」脚注

---

## 四、P2 笔误

| 位置 | 错误 | 修正 |
|------|------|------|
| §1.2 | Categories Filte | Categories Filter |
| §1.2 | `筛选）r` | `筛选）` |
| §2.4-5 | `。。` | `。` |
| §4.1 未登录菜单 | addresssUrl | addressesUrl（与已登录 addressUrl 统一） |
| §4.6 | l3CategoriID | l3CategoryId |
| §4.9 BR622 | 发货挡位 | 发货档位 |
| §4.7.4 | 无游客登录 | 无游客加购 |
| §4.1 BR832 | Added to cart successfully | 与 §4.9.1 统一（successfully / successfully 二选一） |

---

## 五、§A 必改段落（可直接粘贴飞书）

### A.1 文档基础信息（替换表头）

| 时间 | 版本号 | 变更人 | 主要变更内容 |
|------|--------|--------|-------------|
| 2026/09/09 | v1.0 | 廖炫尧 | 首版 |
| 2026/09/20 | v1.1 | 廖炫尧 | Sort By 四档（下期）；运费三档；地址空态；Sticky；Tag 跳专题；搜索去 Filter |
| 2026/09/20 | v1.1.1 | 廖炫尧 | 购物车 50 SKU 上限 |
| 2026/09/21 | v1.1.3 | 廖炫尧 | Add to Cart + /cart 须登录；取消游客车 |
| 2026/09/21 | v1.2 | 廖炫尧 | 订单详情跳本单 orderId；地址三行截断；抽屉 L1 不跳转；分类页 Filter |

---

### A.2 §1.2 本文档范围（**只保留一张表**）

| 模块 | 页面/组件 | 本期范围说明 |
|------|----------|-------------|
| 全局 | 顶部导航、信任背书、底部信息区、右侧 Sticky（#40） | P0 |
| 首页 | 核心展示区、服务介绍 | P0 |
| 搜索 | 搜索弹窗、结果列表、排序 | P0；不含 Featured Tab、类目 Filter |
| 分类 | 一级/二级分类、Categories Filter、分类商品列表 | P0；L1 页 Filter=L2（选中→跳转 L2 页）；L2 页 Filter=L3（选中→本页 ?l3= 筛选） |
| 商品 | 商品详情、规格选择器、推荐组件 | P0 |
| 店铺 | 店铺页、商品 Tab、店铺介绍弹框 | P0 |
| 交易 | 购物车、结算、结果页、地址弹框 | P0；须登录加购/进 cart |
| 专题 | 品牌馆、国家馆、精选、潮流 | P0 |

---

### A.3 §2.4 第 5、6 点（替换）

```
5. 分期与筛选：
  本期做：搜索排序（与 App 一致：From Z-A / A-Z / Newest / Oldest）；分类页 Categories Filter（L1 页→L2 跳转；L2 页→L3 本页 ?l3= 筛选）；店铺 Items Tab 排序。
  本期不做：搜索页 Featured Tab、搜索页类目 Filter、Recommend/New Arrival/Price/Sales 四档排序（见 §6.2）；价格/颜色/尺码/品牌等多维 Filter；优惠券（§6.1）。

6. 购物车：须登录方可 Add to Cart 与进入 /cart（BR852）；不做游客购物车（无 localStorage、无登录合并）；数据仅存登录账号；SKU 上限 50（§4.9.1）。
```

---

### A.4 §3.2 业务目标 — 业务目标行（替换）

```
支持孟加拉本地地址与 COD；Web 加购/购物车须登录，购物车仅存账号（不做游客 localStorage/合并）。
```

---

### A.5 §3.4 跨模块衔接（**建议整表替换，删除重复行**）

| 场景 | 规则 |
|------|------|
| 未登录 Add to Cart | `/login?redirect={当前页 URL}`（BR619）；回跳保留已选 SKU |
| 未登录访问 /cart 或点击 Header/Sticky 购物车 | `/login?redirect=/cart`（BR852） |
| 未登录访问 /checkout、订单、地址 | `/login?redirect={当前 URL}`（BR619） |
| Categories 抽屉 L1 | 不跳转；仅联动 L2 + Recommend |
| 抽屉 L2 View All | → `/category/{l1Id}` |
| 抽屉 L2 类目项 | → `/category/{l2Id}` |
| 次导航 L1 快捷入口 | → `/category/{l1Id}` |
| L1 分类页 Filter 选 L2 | → `/category/{l2Id}` |
| L2 分类页 Filter 选 L3 | 本页 `?l3={l3Id}` 筛选 |
| PDP Add to Cart | 须登录；满 50 SKU 且新 skuId → BR848 |
| 购物车 Checkout | 用户在 /cart 已登录；`Checkout ({selectedQuantity})` → /checkout |
| Buy Now | 须登录；2 步条 Checkout→Complete |
| 登录回 checkout 无地址 | 空态 + Add New Address，不自动弹窗 |
| 退出登录 | 清会话；角标归零；跳转 `/` |

---

### A.6 §4.1 关键修正（片段）

**删除整句：** 「与 §4.6 抽屉内 L1 点击、L2 View All 目标页一致。」

**BR105 验收标准替换为：**
```
展开三列抽屉；L1 不跳转；L2 View All→/category/{l1Id}；L2 项→/category/{l2Id}；次导航 L1→/category/{l1Id}
```

**登录态设计-2 / 状态切换-2 替换：**
```
退出登录：清除会话；Header 未登录态；购物车角标归零；跳转首页 /（BR821）。不做游客购物车合并。
```

**业务规则-6 替换：**
```
退出登录：清除会话并跳转首页 /（非登录页）。
```

---

### A.7 §4.5 搜索排序（本期定稿）

**4.5.1 第 3 点替换：**
```
3. 结果排序（本期与 App 一致）：默认 From Z-A；选项 From Z-A、From A-Z、Newest first、Oldest first（BR211）。Recommend/New Arrival/Price/Sales 四档见 §6.2 下期。
```

**sort 枚举（本期有效）— 替换原表：**

| 枚举值 | 英文展示 | 说明 |
|--------|---------|------|
| name_desc | From Z-A | **默认** |
| name_asc | From A-Z | 名称升序 |
| new_arrival_desc | Newest first | 更新时间倒序 |
| new_arrival_asc | Oldest first | 更新时间正序 |

**删除或移入 §6.2：** recommend / price_* / sales_* 整表（下期）。

---

### A.8 §4.6 路由约定（替换 bullets）

```
- 一级页：/category/{l1Id}
- 二级页：/category/{l2Id}
- 抽屉 L1：不跳转
- 抽屉 L2 View All → /category/{l1Id}
- 抽屉 L2 类目项 → /category/{l2Id}
- 次导航 L1 → /category/{l1Id}
- L3 无独立路由；L2 页内 ?l3={l3Id} 筛选
```

**业务规则 ①–⑤ 替换：**
```
① 抽屉三列，不含 L3 列；
② L1 页 Filter 展示 L2，选中跳转 L2 页；
③ L2 页 Filter 展示 L3，选中写 ?l3= 本页筛选；
④ ?l3= 与 URL 同步，Clear 清除；
⑤ Recommend 固定 10。
```

**删除：** 【下期】BR833；保留 BR836、BR308、BR308a。

---

### A.9 §4.7.2 页面模块（替换一句）

```
须登录方可 Add to Cart（BR619/BR852）与 Buy Now（BR619）。商品下架/不存在走 BR409。
```

**删除 §4.7.11 内整段 BR 交互表**（保留字段表 + topicUrl）。

**删除 BR851 及 BR848–850 在 4.7.4 的重复块中仅保留与 PDP 相关的 BR406/407/852/848。**

---

### A.10 §4.9.6 地址已填态 + BR620b（替换）

```
已填态 — 三行只读卡片（BR620b）：
  第1行：Name + Phone（+880）
  第2行：Address 详细地址（单行/最多2行，超出 ellipsis；Hover 可看全文）
  第3行：Division · District · Area
Change 列表 Modal 每项同三行；可选中 / Edit / Delete。
```

| BR620b | 地址展示态 | Save/Change 后 | 三行布局；地址 truncate | 同 Change 列表 |

---

### A.11 §4.9.6 Order Summary（替换）

```
Subtotal + Promotion + Shipping Fee（按档位动态）+ COD Handling Fee（划线原价 + Free）= Payable Amount。
本期不含 Coupon 行。
```

---

### A.12 §7.1 验收（删除矛盾项后的核心清单）

见 §C.4。

---

## 六、§B 建议删除的整块内容

1. §1.2 第二个重复模块表  
2. §3.4 旧「分类一级收敛」「PDP 加购游客」行  
3. §4.7.11 误贴的 BR401–BR421 表  
4. §4.9.1 规则 16（登录合并）  
5. §4.9 BR851 行  
6. §4.9.6 「本期不做优惠券」下方的完整 Coupon Drawer 规格（移至 §6.2）  
7. §4.9.8 底部重复的 BR620a–c 块（与上文合并）  
8. §4.9.4 内「暂时无法在飞书文档外展示此内容」占位（换真实表格或写「见附录 D」）  
9. §7.1「分类页均不展示 Filter」「游客购物车合并」两条  

---

## 七、§C 附录 — 干净版验收清单（§7.1 可整段替换）

```
[] 搜索页：Sort By 仅 From Z-A / A-Z / Newest / Oldest；无 Filter/Featured
[] 分类页：L1 Filter→L2 跳转；L2 Filter→L3 ?l3= 筛选；抽屉 L1 不跳转
[] 加购/cart：未登录 Add to Cart、/cart、购物车 Icon 均跳转 login；无游客车
[] 购物车：≤50 SKU；Checkout ({n})；满额/接近上限 Banner（英文 Copy）
[] 结算：地址三行+截断；无地址不提交；运费三档动态；COD 划线 Free；无 Coupon UI
[] 结果页：View Order Details→本单 orderId（BR853）；View Order List
[] 全局：Sticky 导航+回顶；topicTag→专题页
[] 退出登录→/；不做游客合并
```

---

*审查完成。按 P0→P1→P2 顺序改完后，建议全文搜索：`游客`、`规则 9`、`两列`、`L1 点击`、`合并`、`Filte`、重复 `BR211`。*
