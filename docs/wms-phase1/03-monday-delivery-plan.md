# 仓库管理 · 设计进程（目标：周一 PRD + 原型）

日期：2026-08-19（周三）  
交付日：2026-08-24（周一）  
交付物：仓库管理 PRD 成稿 + 可演示原型（不写业务代码）

## 1. 交付定义（周一「完成」长什么样）

评审对象能在 30–40 分钟内过完，并当场拍板「可以进入实现计划」。

| 交付物 | 合格标准 |
|---|---|
| PRD | [05-warehouse-prd.md](./05-warehouse-prd.md) 按三大模块写满字段、校验、空态、契约；周一确认 Q1–Q8 后升 v1.0 |
| 原型 | 主路径可点：仓库列表 → 创建仓 → 库区与库位到列层 → SKU 库存视角 → 库存分布详情 → 流水四 Tab；无盘库/退货/报损作业入口 |
| 契约一页纸 | 入出库实习生能按写账时点与库区约束开工，不反向改本模块口径 |
| 待决清单 | 仅剩规格未写死的补强项；已拍板项不再讨论 |

原型工具建议：Figma 或 MasterGo。本仓库只放规格与大纲，**不提交前端工程**。

## 2. 日程（按工作日，周末可压缩）

### 周三 8/19（今天）· 对齐与范围冻结 — 本文档包

- 系统信息梳理、模块分析、进程计划（已完成）。
- 导航已按 PRD 冻结：一级「库存管理」；二级三大模块「仓库管理 / 仓库查询 / 库存流水」。
- 把「跨模块契约」发给两名实习生，要求他们周五前不要发明第三套数量口径。

产出：系统梳理、模块分析、进程计划；PRD v0.9 正文已按三大模块起草（`05-warehouse-prd.md`）。

### 周四 8/20 · PRD 审阅与补文案

对照 `05-warehouse-prd.md` 通读，只补周一待决项与实习生未完成单据状态名，不重开已拍板结论，不写入出库作业步骤。

当日结束标准：另一人只看 PRD 能画出全部页面，无需再问「库位怎么唯一」。

### 周五 8/21 · 原型 v1（主路径可点）

页面顺序（对齐 PRD 三大模块）：

1. 一级导航：库存管理 / 入库 / 出库；库存管理内二级：仓库管理、仓库查询、库存流水。入库/出库仅占位。
2. 仓库列表（空态 + 有仓）。
3. 创建仓库、编辑仓库。
4. 删除拦截（有库存 / 有未完成单据）。
5. 库区与库位：三类库区树 + 新建到列层 + 唯一性错误；报损区无作业按钮。
6. 仓库查询 · SKU 库存视角（实物 100 / 锁定 2 / 可用 98）；Product Tab 一帧。
7. 库存分布详情：仓 → 区 → 货架-列-层；收货区标明不参与推荐。
8. 库存流水四 Tab：入库 / 出库占用 / 出库扣减 / 解冻，各一条示意（含关联单号、变更后三量）。

当日结束标准：用原型可以把验收句「能建到列层、两种视角、流水能追溯」演示出来（查询数字用静态示意）。

### 周六 8/22 · 对照规格扫一遍（可半天）

对照检查表（全部应为「是」或「明确非本模块」）：

- [ ] 库位五段，没有把「库位」画成只到货架。
- [ ] 三类库区都在；报损区无作业。
- [ ] 实物 = 锁定 + 未锁定；账面 = 实物 + 在途；在途不出现在库位行。
- [ ] 无盘库、退货、无单入库、回写电商、路径优化入口。
- [ ] 采购单没有出现在库存管理一级菜单。
- [ ] 出库推荐约束写在契约里，页面上不做出库推荐交互。
- [ ] 期初一次的控制不画在仓库表单里（只读「已期初」标记即可）。

同时收集实习生对「未完成单据」定义的书面回复，写入 PRD 附录。

### 周日 8/23 · 打磨与讲稿

- 补空态、校验文案、下钻示意数据（同一个 SKU 贯穿查询与流水，便于周一讲解）。
- 准备 10 分钟讲稿：系统边界 2 分钟 → 主数据 4 分钟 → 查询口径 3 分钟 → 给实习生的契约 1 分钟。
- PRD 版本改为 v1.0 待确认。

### 周一 8/24 · 评审交付

议程建议：

1. 重申不做清单（防止评审扩一期）。
2. 走原型主路径。
3. 过契约表，让入出库负责人当场点头。
4. 只讨论待决项（见第 4 节），已拍板项不重开。
5. 确认「批准后才写业务代码」。

## 3. 每天的最小产出（防止摊开）

```text
Wed  文档包（范围冻结）
Thu  PRD 字段+规则+契约
Fri  可点击原型主路径
Sat  规格对照 + 实习生对齐未完成单据
Sun  讲稿 + 示意数据一致
Mon  评审，记录确认项
```

若周四 PRD 未写完，周五不要先画无关视觉；先保证库位五段与查询三量画对。

## 4. 允许在周一拍板的待决项（规格未写死）

这些可以设计建议，但必须在评审里口头确认，**不要当成已经推翻或修改规格**：

| 编号 | 待决 | 建议默认 |
|---|---|---|
| Q1 | 库位是否需要独立编码字段 | 要；由五段生成，全局唯一，可复制 |
| Q2 | 仓库名称是否唯一 | 唯一 |
| Q3 | 库区类型能否修改 | 不能 |
| Q4 | 有库存库位能否改货架/列/层 | 不能改层级段；避免账记到「已改名的另一个位置」 |
| Q5 | 空库位能否删除 | 能；有库存或被未完成单据引用则不能 |
| Q6 | 新建仓库是否默认生成三类空库区 | 要；库位仍手建，避免自动造大量空位 |
| Q7 | 一期是否做按钮级权限 | 不做细权限也可；文档仍按运营维护、仓管查询来写主路径 |
| Q8 | 草稿采购是否计入在途 | 不计 |

已拍板、周一 **禁止** 重开的：系统独立、不建品、不回写可售、日常入库必须采购单、每仓一次期初、出库先预检后冻结、拣货才扣减、打包不改库存、推荐只走存储区。

## 5. 工作方式约束

- 未获同意不写 WMS 业务代码（含本仓库前端/后端）。
- 不把入出库作业页面画进本模块原型（导航可占位）。
- 查询页可以用静态数字演示口径，不需要接真实接口。
- 给实习生的是契约与对象，不是让他们改仓库层级。

## 6. 周一之后（仅规划，不是本周任务）

评审通过 → 另开实现计划（接口、表结构、任务拆分）。实现计划仍须单独批准后再写代码。库存查询与流水的真实数据验收排在入出库写账之后。
