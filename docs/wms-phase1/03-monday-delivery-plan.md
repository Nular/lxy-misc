# 本侧模块 · 设计进程（目标：周一 PRD + 原型）

日期：2026-08-20  
交付日：2026-08-24（周一）  
交付物：PRD v0.10 基础上的评审稿 + 可演示原型（不写业务代码）  
正文：[05-warehouse-prd.md](./05-warehouse-prd.md)

## 1. 交付定义

| 交付物 | 合格标准 |
|---|---|
| PRD | v0.10：设置（仓库/库区）、库存查看（SPU/SKU）、流水五 Tab |
| 原型 | 建仓（停用+3 默认库区）→ 双击库区数量管库区 → 加一个后五类区 → SPU/SKU 查看（无在途账面、无列层）→ 流水含采购单 Tab |
| 契约 | 入出库按库区写账；采购单模块写采购单流水 |

## 2. 日程

### 周三 8/19

原 v0.9（列层、三类区）已完成；已被周四变更取代。

### 周四 8/20

PRD 按需求变更改至 v0.10。发给入出库：记账只到库区；采购单负责人：须写采购单流水。

### 周五 8/21 · 原型 v1

1. 侧栏：设置 → 仓库管理、仓库库区管理；另有库存查看、库存流水。
2. 仓库列表（ID、属性、状态 Tag、库区数量）；空态 + 有仓。
3. 创建仓库全字段；提交后停用 + 3 个默认库区。
4. 编辑页管理员启用；双击库区数量进入库区列表。
5. 库区列表四列；添加打包区等；删除默认区被拦截。
6. 库存查看 SPU / SKU；100 / 2 / 98；主条码列可空。
7. 分布详情只到仓库、库区。
8. 流水五 Tab，采购单 Tab 无「创建采购单」。

### 周六 8/22

- [ ] 无货架/列/层
- [ ] 八类库区能力；默认三区不可删
- [ ] 无在途、账面
- [ ] 无盘库、退货作业、报损作业、回写电商
- [ ] 采购单作业不在本侧导航
- [ ] 冻结区与库存锁定不是同一入口

### 周日–周一

讲稿：设置主数据 → 库存查看口径 → 流水与采购单边界。评审确认默认可编辑不可删、停用仓规则。禁止重开独立 WMS / 不建品 / 不回写 / 入库须采购单 / 期初一次 / 预检后占用 / 拣货扣减。

## 3. 周一可确认项

| 编号 | 项 | 文档默认 |
|---|---|---|
| D1 | 三个默认库区 | 可编辑、不可删除，不需要则停用 |
| D2 | 同仓同类型 | 至多一个，总数最多 8 |
| D3 | 停用仓/区 | 不可作为新作业目标 |
| D4 | 采购单流水事件 | 创建/确认/回写已收/完成/关闭 |
| D5 | 「动街区」 | 按冻结区收录 |

## 4. 约束

未批准不写业务代码。原型不画采购单、入出库作业页。不把货架列层画回来。
